# ===========================================
#  .dockerignore — BACKEND
# ===========================================
#  Excluimos lo que no necesita la imagen de Node:
#  node_modules (se instala en el build), .env (va
#  por variables de entorno), archivos de otros
#  servicios, y basura del sistema.
# ===========================================

node_modules
npm-debug.log
.env
.env.local
*.md
.git
.gitignore
docker
python-services
FrontEnd
